CREATE TABLE helpdesk_user.temp_table_demo (
  2      demo_id        NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  3      demo_name      VARCHAR2(100) NOT NULL,
  4      created_on     DATE DEFAULT SYSDATE
  5  );
